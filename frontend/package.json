{
  "name": "vibespeak-frontend",
  "version": "0.1.0",
  "description": "Vibespeak frontend with Tailwind CSS",
  "scripts": {
    "build-css": "tailwindcss -i input.css -o styles.css --minify",
    "watch-css": "tailwindcss -i input.css -o styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "@tailwindcss/forms": "^0.5.7"
  }
}